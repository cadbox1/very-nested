import React from "react";
import { HashRouter as Router } from "react-router-dom";
import { Manager } from "./Manager";

const App = () => (
	<Router>
		<Manager />
	</Router>
);

export default App;
