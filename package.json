{
  "name": "very-nested",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-scripts",
      "**/react-scripts/**"
    ]
  },
  "scripts": {
    "prepare": "lerna run prepare"
  },
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
